# Shared defaults for the automation framework
selenide.timeout=10000
selenide.pageLoadTimeout=30000
selenide.reportsFolder=build/reports
selenide.headless=false

# API endpoints (can be overridden per environment via application-<env>.properties)
api.baseUrl=https://localhost/api
ui.baseUrl=https://localhost/ui
