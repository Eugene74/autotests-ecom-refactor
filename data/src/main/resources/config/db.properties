# Database vendor selection
db.vendor=oracle

# Oracle connection defaults
db.oracle.driver=oracle.jdbc.OracleDriver
db.oracle.url=jdbc:oracle:
db.oracle.username=ec
db.oracle.password=qwerty

# PostgreSQL connection defaults
db.postgres.driver=org.postgresql.Driver
db.postgres.url=jdbc:postgresql://localhost:5432/postgres
db.postgres.username=postgres
db.postgres.password=postgres

# Legacy keys retained for backward compatibility
DB_CONNECTION_TP2ST=jdbc:oracle:
DB_USER_OWNER=ec
DB_PASSWORD=qwerty
